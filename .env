# Environment variables for docker-compose
# Define MYSQL_ROOT_PASSWORD to avoid docker-compose warning about missing variable
MYSQL_ROOT_PASSWORD=root123
# Optional: override database host/port (points to the service name in compose)
DATABASE_HOST=database
DATABASE_PORT=3306
# Useful for local dev to match skeleton/.env
APP_ENV=dev
APP_SECRET=talanaTrivia2025
